function CreateAccount(name,surname,birthDay,birthMonth,birthYear,email,userID,password){
    this.name = name,
    this.surname = surname,
    this.birthDay = birthDay,
    this.birthMonth = birthMonth,
    this.birthYear = birthYear,
    this.email = email,
    this.userID = userID,
    this.password = password
}
